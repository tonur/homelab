{{- $defaults := .Values.defaults -}}
{{- range $name, $config := .Values.servers }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ $name }}
  template:
    metadata:
      labels:
        app: {{ $name }}
    spec:
      securityContext:
        fsGroup: {{ $defaults.pgid }}
        runAsUser: {{ $defaults.puid | int }}
        runAsGroup: {{ $defaults.pgid | int }}

      initContainers:
      # STAGE 1: Fix volume permissions (Runs as Root)
      - name: filesystem-prepare
        image: busybox
        securityContext:
          runAsUser: 0 
        command:
        - sh
        - -c
        - |
          echo "Preparing directory structure..."
          mkdir -p /data/server
          chown -R {{ $defaults.puid }}:{{ $defaults.pgid }} /data
        volumeMounts:
        - name: game-storage
          mountPath: /data

      # STAGE 2: Download/Update Game Files (Runs as Steam User)
      - name: steam-updater
        image: {{ $defaults.image }}
        command: ["/usr/games/steamcmd"]
        args:
          - "+@sSteamCmdForcePlatformType"
          - "windows"
          - "+force_install_dir"
          - "/data/server"
          - "+login"
          - "anonymous"
          - "+app_update"
          - {{ $config.appId | quote }}
          - "validate"
          - "+quit"
        volumeMounts:
        - name: game-storage
          mountPath: /data

      # STAGE 3: Run the Game (Runs as Steam User)
      containers:
      - name: game-runtime
        image: {{ $defaults.image }}
        command:
        - bash
        - -c
        - |
          echo "Starting {{ $name }} Dedicated Server..."
          # 'exec' ensures the game process receives SIGTERM for graceful shutdowns
          exec xvfb-run --auto-servernum {{ $config.startCommand }}
        env:
        - name: TZ
          value: {{ $defaults.tz | quote }}
        {{- range $config.extraEnvs }}
        - name: {{ .name }}
          value: {{ .value | quote }}
        {{- end }}
        volumeMounts:
        - name: game-storage
          mountPath: /data
      volumes:
      - name: game-storage
        persistentVolumeClaim:
          claimName: {{ $name }}-data-pvc
{{- end }}
