{{- if .Values.jsonResume.enabled }}
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: json-resume
  namespace: apps
spec:
  interval: 5m
  chart:
    spec:
      chart: ./charts/json-resume
      version: "*"
      sourceRef:
        kind: GitRepository
        name: homelab
        namespace: default
      interval: 1m
      reconcileStrategy: Revision # Local chart
  install:
    remediation:
      retries: 3
  upgrade:
    force: true
    remediation:
      retries: 3
  values:
    # Override values if needed
    ingress:
      host: kragh.dev

    webServer:
      replicas: 1
{{- end }}