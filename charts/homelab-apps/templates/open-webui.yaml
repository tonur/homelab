{{- if .Values.openWebUI.enabled }}
---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: open-webui
  namespace: apps
spec:
  interval: 24h
  url: https://open-webui.github.io/helm-charts
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: open-webui
  namespace: apps
spec:
  interval: 5m
  chart:
    spec:
      chart: open-webui
      version: "14.4.0"
      sourceRef:
        kind: HelmRepository
        name: open-webui
        namespace: apps
      interval: 1m
      reconcileStrategy: Revision
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: -1
  values:
    extraEnvVars:
      - name: OPENAI_API_BASE_URL
        value: "http://litellm.apps.svc.cluster.local:4000/v1" 
      - name: OPENAI_API_KEY
        valueFrom:
          secretKeyRef:
            name: litellm-secrets 
            key: LITELLM_MASTER_KEY
    ollama:
      enabled: false
    ingress:
      enabled: true
      class: "traefik"
      host: "open-webui.kragh.dev"
      tls: true
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
{{- end }}
