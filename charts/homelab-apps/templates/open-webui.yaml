---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: open-webui
  namespace: apps
spec:
  interval: 5m
  chart:
    spec:
      chart: ./charts/open-webui
      version: "*"
      sourceRef:
        kind: GitRepository
        name: homelab
        namespace: default
      interval: 1m
      reconcileStrategy: Revision
  install:
    remediation:
      retries: 3
  upgrade:
    force: true
    remediation:
      retries: 3
