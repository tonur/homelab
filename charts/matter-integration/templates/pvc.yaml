apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matter-server-data
  labels:
    app: matter-server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path