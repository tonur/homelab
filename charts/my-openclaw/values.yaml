# Default values for my-openclaw
image:
  repository: ghcr.io/<username>/openclaw
  tag: latest
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
ingress:
  host: oidc.kragh.dev
  tls:
    enabled: true
    certResolver: tailscale
replicas: 1
