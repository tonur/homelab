---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: suwayomi
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: suwayomi
  template:
    metadata:
      labels:
        app.kubernetes.io/name: suwayomi
    spec:
      containers:
      - name: suwayomi
        image: ghcr.io/suwayomi/suwayomi-server:stable
        ports:
        - containerPort: 4567
        env:
        - name: TZ
          value: "Europe/Copenhagen"
        - name: SERVER_OPDS_CBZMIMETYPE
          value: "LEGACY"
        - name: SERVER_OPDS_CBRMIMETYPE
          value: "LEGACY"
        - name: AUTH_MODE
          value: "basic_auth"
        - name: AUTH_USERNAME
          value: "admin"
        - name: AUTH_PASSWORD
          valueFrom:
            secretKeyRef:
              name: suwayomi-secrets
              key: ADMIN_PASSWORD
        volumeMounts:
        - name: data
          mountPath: /home/suwayomi/.local/share/Tachidesk
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: suwayomi-data
