---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Homelab cluster configuration managed by Flux GitOps
# This is applied by Flux, not directly by humans

resources:
  # Flux Kustomizations for staged infrastructure deployment
  - flux-kustomizations/
